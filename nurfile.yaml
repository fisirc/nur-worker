name: nur-worker
language: rust
build:
  command: cd examples/hello_world && cargo build --target wasm32-unknown-unknown --release
  output: examples/hello_world/target/wasm32-unknown-unknown/release/hello_world.wasm
